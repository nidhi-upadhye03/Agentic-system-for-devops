apiVersion: v1
kind: Service
metadata:
  name: approval-frontend
  namespace: ai-system
  labels:
    app: approval-frontend
    tier: frontend
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: http
    protocol: TCP
    name: http
  selector:
    app: approval-frontend
