apiVersion: v1
kind: Service
metadata:
  name: worker-service
  namespace: ai-system
  labels:
    app: worker-service
    tier: backend
spec:
  type: ClusterIP
  ports:
  - port: 9090
    targetPort: metrics
    protocol: TCP
    name: metrics
  selector:
    app: worker-service
