apiVersion: v1
kind: Service
metadata:
  name: approval-backend
  namespace: ai-system
  labels:
    app: approval-backend
    tier: backend
spec:
  type: ClusterIP
  ports:
  - port: 3000
    targetPort: http
    protocol: TCP
    name: http
  selector:
    app: approval-backend
